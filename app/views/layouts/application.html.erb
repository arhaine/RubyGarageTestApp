<!DOCTYPE html>
<html>
<head>
  <title>RubyGarageTaskManager | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar">
  <div class="navbar-inner">
      <nav class="nav">
          <h4 class="navbar-brand"><%= link_to "Ruby Garage Task Manager", root_path %></h4>
      </nav>
      <nav class="nav pull-right">
          <span class="btn-group">
          <%= yield(:controls) %>
          <% if current_user %>
              <%= link_to "Sign Out", signout_path, class:"btn", method: "delete" %>
          <% else %>
              <%= link_to "Sign In", signin_path, class:"btn" %>
              <%= link_to "Sign Up", signup_path, class:"btn" %>
          <% end %>
          </span>
      </nav>
  </div>
</div>


<div class="container">
  <%= yield %>
</div>
</body>
</html>
